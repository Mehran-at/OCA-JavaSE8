package com.oca.inheritance.sixtyeight;

abstract class A {

    abstract long count(); //Line n1. package private access modifier and needs the exam primitive data type as return
    // type when gets overridden

    abstract Object get(); //Line n2  package private access modifier and needs either the exam Object data type or
    // its subtype as return type when gets overridden
}
