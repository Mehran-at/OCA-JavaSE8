package com.oca.inheritance.thirtysix;

class C /* extends B */ implements I {

    //  Static and non-static methods with same signature are not allowed in one scope, therefore class C fails to compile.
}
